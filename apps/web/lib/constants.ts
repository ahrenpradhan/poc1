// Pagination
export const USER_MESSAGE_PAGE_SIZE = 10;

// UI Delays (ms)
export const VIEWPORT_FILL_DELAY = 100;
export const VIEWPORT_FILL_INTERVAL = 50;
export const REFOCUS_DELAY = 0;

// Intersection Observer
export const INTERSECTION_THRESHOLD = 0.1;

// Breakpoints (matches Tailwind defaults)
export const BREAKPOINTS = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
} as const;

// Sidebar
export const SIDEBAR_WIDTH = 256; // w-64 = 16rem = 256px

// Message constraints
export const MESSAGE_MAX_WIDTH_MOBILE = "95%";
export const MESSAGE_MAX_WIDTH_DESKTOP = "80%";

// Input constraints
export const INPUT_MAX_HEIGHT = 200;
export const INPUT_MAX_HEIGHT_MOBILE = 120;
